[tool.poetry]
name = "codetheory"
version = "0.1.0"
description = ""
authors = ["Оникс <onyxservers@yandex.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
numpy = "^2.1.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
